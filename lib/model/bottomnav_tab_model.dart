import 'package:flutter/material.dart';

class BottomNavTab {
  String title;
  List<Widget> tabs;
  Color color;
  List<Widget> buttons;

  BottomNavTab(this.title, this.tabs, this.color, this.buttons);
}
